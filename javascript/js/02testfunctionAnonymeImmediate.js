/**
 * Fonction anonyme immédiate (IIFES)
 * Permet d'isoler le code, c'est à dire que les 
 * variables sont locales à la fonction anonyme
 */
(function() {
  var j = 50;
  let i = 12;
})(); // La fonction est appelée directement 

